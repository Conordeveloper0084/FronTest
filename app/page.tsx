import { TelegramInterface } from "@/components/telegram-interface"

export default function TelegramApp() {
  return (
    <main className="h-screen w-full bg-[#f0f2f5] flex items-center justify-center overflow-hidden">
      <TelegramInterface />
    </main>
  )
}
